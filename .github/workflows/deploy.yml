name: deploy
on:
  push:
    paths:
      - '*/**'
    branches:
      - main

jobs:
  deploy:
    name: Deploy to server
    runs-on: ubuntu-latest
    # needs: build
    environment:
      name: 'PROD'
    steps:
      - name: Setup SSH
        uses: actions/checkout@v3
        run: |
          eval $(ssh-agent -s)
          ssh-add <(echo "${{ secrets.PRIVATE_KEY }}")
          mkdir -p /home/runner/.ssh && chmod 700 ~/.ssh
          ssh-keyscan $HOST_ADDRESS >> ~/.ssh/known_hosts
          ls -la /home/runner && ls -la /home/runner/.ssh
          ssh $USER@$HOST_ADDRESS 'export GHCR_USERNAME=${{ github.repository_owner }} GHCR_TOKEN=${{ secrets.GITHUB_TOKEN }}; bash -s' < deploy-container.sh
        working-directory: scripts/
      - name: Check Deployment Status
        run: |
          if [ $? -ne 0 ]; then
            echo "Deployment failed"
            exit 1
          fi
